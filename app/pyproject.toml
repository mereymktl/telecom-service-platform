[project]
name = "multi-cloud-devops-demo"
version = "1.0.0"
description = "Multi-cloud DevOps portfolio demo"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.111", "uvicorn>=0.29", "prometheus-client>=0.20", "pydantic>=2.7"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "httpx>=0.27", "ruff>=0.4"]